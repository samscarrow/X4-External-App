{
  "name": "x4-cocaptain-mcp",
  "version": "0.1.0",
  "description": "Standalone stdio MCP server exposing X4-External-App live game data, the savegame SQLite database, and an event long-poll — the read-only Phase 1 of the X4 co-captain.",
  "type": "module",
  "main": "index.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node index.js",
    "smoke": "node smoke.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "better-sqlite3": "^11.7.0",
    "zod": "^3.24.0"
  }
}
