{
  "name": "x4-cocaptain-mcp",
  "version": "0.5.0",
  "description": "Standalone stdio MCP server exposing X4-External-App live game data, the savegame SQLite database, a severity-tiered event long-poll, text-to-speech, and the in-game command queue \u2014 the X4 co-captain bridge.",
  "type": "module",
  "main": "index.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node index.js",
    "smoke": "node smoke.js",
    "test": "node --test"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "better-sqlite3": "^11.7.0",
    "zod": "^3.24.0"
  }
}
